subconfigs:
    - model: base
    - training: base
    - data: base
flyconfig:
    run:
        # Specify the working directory
        # ./outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
        dir: ./../outputs/${task.name}/${now:%H-%M-%S}
task:
    name: knowledge-dialog